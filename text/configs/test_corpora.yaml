# Registre des corpus de test (hors domaine BTP).
# Convention : dataset/test/data_<id>.csv
# Embeddings test : embeddings/test/Qwen3-Embedding-0.6B__<id>.csv (double _ avant l'id)

default: metallurgie

corpora:
  metallurgie:
    display_name: Métallurgie
    data_csv: dataset/test/data_metallurgie.csv
    emb_csv: embeddings/test/Qwen3-Embedding-0.6B__metallurgie.csv
    prompt_context: metallurgie

  # Exemple pour un second corpus (décommenter après ajout des fichiers) :
  # chimie:
  #   display_name: Chimie
  #   data_csv: dataset/test/data_chimie.csv
  #   emb_csv: embeddings/test/Qwen3-Embedding-0.6B__chimie.csv
