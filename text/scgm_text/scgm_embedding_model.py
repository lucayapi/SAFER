"""Removed: use SCGMTextModel (end2end) instead."""

raise ImportError(
    "SCGMEmbeddingNet (precomputed embeddings) was removed. "
    "Use scgm_text.scgm_text_model.SCGMTextModel with the end2end pipeline."
)
