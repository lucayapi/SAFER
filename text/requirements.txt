# =============================================================================
# SAFER/text — dépendances Python (Python 3.10 recommandé sur HPC)
#
# Installation (venv vide) :
#   cd text
#   python3 -m venv .venv && source .venv/bin/activate
#   pip install -U pip
#   bash scripts/install_text_venv.sh
#
# Ou manuellement :
#   pip install -r requirements.txt -c constraints.txt
#
# GPU : installer torch CUDA APRÈS (voir scripts/install_text_venv.sh).
# =============================================================================

# --- Cœur scientifique (numpy < 2 pour gensim, numba, umap, pgmpy 0.1.x) ---
numpy==1.26.4
pandas==2.2.3
scipy==1.13.1
scikit-learn==1.5.2
joblib==1.4.2
pyyaml==6.0.2
tqdm==4.67.1
packaging>=24.0

# --- PyTorch (CPU sur PyPI ; remplacer par roue CUDA sur cluster) ---
torch==2.5.1

# --- Hugging Face : Qwen3-Embedding exige transformers >= 4.51 ---
# Versions figées aussi dans constraints.txt (évite retour à 4.46.x).
transformers==4.51.3
tokenizers==0.21.0
accelerate==1.1.1
safetensors>=0.4.3,<1.0
huggingface-hub>=0.26.0,<1.0
fsspec>=2023.1.0,<=2024.9.0

# --- Contrastifs legacy (batch triplet, SoftTriple, SupCon) ---
sentence-transformers==3.4.1
datasets==3.2.0

# --- Visualisation ---
matplotlib==3.9.4
seaborn==0.13.2
plotly==5.24.1
pyarrow==17.0.0

# --- Jupyter ---
jupyter==1.1.1
ipykernel==6.29.5
ipython==8.18.1
papermill==2.6.0

# --- Cartographie (notebook) ---
umap-learn==0.5.7
datamapplot==0.6.4

# --- OpenAI & env ---
openai==1.59.2
python-dotenv==1.0.1

# --- Topics ---
gensim==4.3.3
bertopic==0.16.4
hdbscan==0.8.40
nltk==3.9.1

# --- Réseaux bayésiens ---
pgmpy==0.1.26
networkx==3.2.1
pyvis==0.3.2

# --- Tests ---
pytest==8.3.4
