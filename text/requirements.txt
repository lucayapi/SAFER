# Dépendances unifiées (SCGM texte, MALT, topic_eval, BN MALT, notebooks).
# Versions figées récentes et stables (Python 3.9–3.12 ; 3.9 : networkx ≤3.2.x).
# Exclut la stack image historique : requirements_legacy_image.txt
#
# NumPy 1.26.x : compatible matplotlib/scipy conda classiques ; pgmpy 0.1.x (pas 1.x qui impose numpy>=2).
# PyTorch : roue CPU par défaut sur PyPI ; pour GPU, réinstaller depuis https://pytorch.org/get-started/locally/

# --- Cœur scientifique & ML ---
numpy==1.26.4
pandas==2.2.3
scipy==1.13.1
scikit-learn==1.5.2
joblib==1.4.2
pyyaml==6.0.2
tqdm==4.67.1

# --- PyTorch (adaptation texte) ; roue CPU — voir site PyTorch pour CUDA ---
torch==2.5.1

# --- Encodeurs texte (input_mode=text, fine-tuning backbone) ---
# Qwen3-Embedding (qwen3) : transformers >= 4.51.0 (voir HF model card).
transformers==4.51.3
tokenizers==0.21.0
accelerate==1.1.1
# Batch triplet / SoftTriple / SupCon (legacy sentence_transformers.SentenceTransformerTrainer)
sentence-transformers==3.4.1
datasets==3.2.0

# --- Visualisation & données tabulaires ---
matplotlib==3.9.4
seaborn==0.13.2
plotly==5.24.1
pyarrow==17.0.0

# --- Jupyter (ipython : dernière 8.x dispo selon interpréteur ; éviter versions inexistantes sur PyPI) ---
jupyter==1.1.1
ipykernel==6.29.5
ipython==8.18.1
papermill==2.6.0

# --- Cartographie & clustering (notebook 03) ---
umap-learn==0.5.7

# --- Thèmes OpenAI & cartes 2D (notebook 01) ---
# 1.59.0 absente de PyPI ; 1.59.2+ OK pour l’API Chat Completions utilisée par openai_theme_labels.
openai==1.59.2
python-dotenv==1.0.1
# 0.7.x peut être indisponible selon la version Python / l’index PyPI ; 0.6.4 reste compatible notebooks.
datamapplot==0.6.4

# --- Cohérence topics (C_v, NPMI) ---
gensim==4.3.3

# --- Comparaison MALT / BERTopic / KMeans (notebook 03) ---
bertopic==0.16.4
hdbscan==0.8.40
nltk==3.9.1

# --- Réseaux bayésiens MALT (notebook 04, bn_malt) ---
pgmpy==0.1.26
# networkx 3.3+ exige Python ≥3.10 ; 3.2.1 reste OK pour 3.9 et pgmpy 0.1.x
networkx==3.2.1
pyvis==0.3.2

# --- Tests (SCGM fidelity / probabilités) ---
pytest==8.3.4
